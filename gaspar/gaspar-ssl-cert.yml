apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: gaspar-tls
spec:
  secretName: gaspar-tls-secret
  dnsNames:
    - www.gaspar.ovh
    - gaspar.ovh
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer