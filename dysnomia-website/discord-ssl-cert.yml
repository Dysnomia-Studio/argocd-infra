apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: discord-tls
spec:
  secretName: discord-tls-secret
  dnsNames:
    - discord.dysnomia.studio
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer