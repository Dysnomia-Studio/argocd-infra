apiVersion: v1
kind: ConfigMap
metadata:
  name: galactae-client-01-config
data:
  config.php: |
    <?php
    $config = array(
      'mongoURL' => '',
      'SQLCredentials' => [
        'host' => getenv('GALACTAE_SQL_SERVER'),
        'db' => getenv('GALACTAE_SQL_DATABASE'),
        'id' => getenv('GALACTAE_SQL_USERNAME'),
        'password' => getenv('GALACTAE_SQL_PASSWORD'),
        'driver' => 'pgsql',
        'port' => 5432
      ],
      "languageList" => [
        'en' => 'English',
        'fr' => 'Francais',
        //'de' => 'Deutsch',
        //'ru' => 'русский'
      ],
      "cacheBlacklist" => [],
      "stats" => [
        'host' => getenv('STATS_SQL_SERVER'),
        'db' => getenv('STATS_SQL_DATABASE'),
        'id' => getenv('STATS_SQL_USERNAME'),
        'password' => getenv('STATS_SQL_PASSOWRD'),
      ],
    );

  gvars.php: |
    <?php
    $cdnURL = "https://cdn.galactae.eu/game/";
    $dbname = "galactae-01";
    $serverName = "milkyway";
    $beginnerProtectionDuration = 7;

    $config['SQLCredentialsGameServer'] = [
        'host' => getenv('GALACTAE_01_SQL_SERVER'),
        'db' => getenv('GALACTAE_01_SQL_DATABASE'),
        'id' => getenv('GALACTAE_01_SQL_USERNAME'),
        'password' => getenv('GALACTAE_01_SQL_PASSWORD'),
        'driver' => 'pgsql',
        'port' => 5432
    ];

    if(!isset($noscript)) {
    ?>
    <script type="text/javascript" src="<?= $cdnURL ?>js/cookies.js"></script>
    <script type="text/javascript">
            let userData = {
                    username: "",
                    id: "",
                    pid: getCookie('galactae-account-pid'),
                    money: 0,
                    gvt: 1,
                    research: {}
            };

            let gameServer = "wss://01-srv.galactae.eu";
            var cdnURL = "<?php echo $cdnURL; ?>";
            let serverName = "<?php echo $serverName; ?>";

            function setGvars(id,user) {
                    userData.username = user;
                    userData.id = id;
            }
    </script>
    <?php
    }
    ?>