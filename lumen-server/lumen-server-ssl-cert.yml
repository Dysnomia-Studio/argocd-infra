apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: lumen-server-tls
spec:
  secretName: lumen-server-tls-secret
  dnsNames:
    - lumen.elanis.eu
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer