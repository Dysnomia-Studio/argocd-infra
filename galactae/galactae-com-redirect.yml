apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: galactae-com-redirect-ingressroute
spec:
  entryPoints:
  - websecure
  routes:
  - match: Host(`galactae.com`)
    kind: Rule
    services:
    - name: galactae-website-svc
      port: 80
    middlewares:
      - name: galactae-com-redirect
  tls:
    certResolver: le
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: galactae-com-redirect
spec:
  redirectRegex:
    regex: ^https?://galactae\.com(.*)$
    replacement: https://galactae.eu${1}
    permanent: true
