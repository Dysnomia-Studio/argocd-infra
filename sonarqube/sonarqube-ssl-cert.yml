apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: sonarqube-tls
spec:
  secretName: sonarqube-tls-secret
  dnsNames:
    - sonar.dysnomia.studio
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer